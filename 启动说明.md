# TestHub 本地启动说明

> 本文档按本机实际环境整理（Windows）。配置以项目根目录 `config.yaml` 为准。

## 服务依赖

| 服务 | 说明 | 端口 | 本机路径 |
|------|------|------|----------|
| MySQL 8.0.46 | 数据库 | 3306 | `D:\mysql-8.0.46-winx64\` |
| Redis 5.0.14 | 缓存 | 6379 | 项目内 `redis-windows\` |
| 后端 | Django + Uvicorn | 8001 | 项目 `venv\` + `start_backend.py` |
| 前端 | Vue3 + Vite | 3002 | 项目 `frontend\` |

说明：`config.yaml` 只提供**连接参数**，不会自动启动 MySQL / Redis，需要先手动拉起。

## 启动顺序

建议顺序：**MySQL → Redis → 前后端**。

### 1. 启动 MySQL

新开一个终端：

```bat
cd /d D:\mysql-8.0.46-winx64\bin
mysqld.exe --console
```

看到类似 `ready for connections ... port: 3306` 即表示成功。

连接信息（与 `config.yaml` 一致）：

- 主机：`127.0.0.1`
- 端口：`3306`
- 用户：`root`
- 密码：`testhub123`
- 数据库：`testhub`

验证：

```bat
D:\mysql-8.0.46-winx64\bin\mysql.exe --user=root --password=testhub123 --host=127.0.0.1 --port=3306 -e "SELECT 1; SHOW DATABASES LIKE 'testhub';"
```

### 2. 启动 Redis

再开一个终端：

```bat
cd /d D:\project\testhub\redis-windows
redis-server.exe redis.windows.conf
```

验证：

```bat
cd /d D:\project\testhub\redis-windows
redis-cli.exe ping
```

应返回 `PONG`。

### 3. 启动前后端

#### 方式 A：一键脚本（推荐，日常开发）

在项目根目录双击或执行：

```bat
cd /d D:\project\testhub
start.bat
```

`start.bat` 会：

1. 从 `config.yaml` 读取 `BACKEND_PORT` / `FRONTEND_PORT`（当前为 8001 / 3002）
2. 新窗口启动前端：`frontend` 下 `npm run dev`
3. 新窗口启动 Django-Q2：`manage.py qcluster`
4. 新窗口启动定时任务：`manage.py run_all_scheduled_tasks`
5. 当前窗口启动后端：`venv\Scripts\python.exe start_backend.py --port 8001`

> `start.bat` **不会**启动 MySQL / Redis，请先完成上面两步。

#### 方式 B：手动分窗口启动

```bat
REM 后端（项目根目录）
cd /d D:\project\testhub
venv\Scripts\python.exe start_backend.py --port 8001

REM 前端（另开终端）
cd /d D:\project\testhub\frontend
npm run dev
```

可选（需要异步任务 / 定时任务时再开）：

```bat
cd /d D:\project\testhub\backend
..\venv\Scripts\python.exe manage.py qcluster

cd /d D:\project\testhub\backend
..\venv\Scripts\python.exe manage.py run_all_scheduled_tasks
```

## 停止服务

按需结束对应进程（不要无脑 `taskkill /IM python.exe`，会误杀其它 Python 程序）：

```bat
REM MySQL
taskkill /F /IM mysqld.exe

REM Redis
taskkill /F /IM redis-server.exe

REM 查看前后端占用端口后按 PID 结束
netstat -ano | findstr "LISTENING" | findstr ":8001 :3002"
taskkill /F /PID <进程ID>
```

前端 Vite 窗口、QCluster / Scheduler 窗口可直接关闭对应 `cmd` 窗口。

## 配置说明

本地配置文件：项目根目录 `config.yaml`（优先级：环境变量 > `config.yaml` > 默认值）。

当前本机关键配置：

```yaml
BACKEND_PORT: 8001
FRONTEND_PORT: 3002

DB_NAME: testhub
DB_USER: root
DB_PASSWORD: testhub123
DB_HOST: 127.0.0.1
DB_PORT: '3306'

REDIS_URL: redis://127.0.0.1:6379/0
```

改密码或端口后，请同步修改 `config.yaml`，并重启后端。

## 访问地址

| 入口 | 地址 |
|------|------|
| 前端 | http://localhost:3002 |
| 后端 API | http://localhost:8001 |
| API 文档 | http://localhost:8001/api/docs/ |
| 管理后台 | http://localhost:8001/admin/ |

默认管理员账号（本机已验证）：`admin` / `admin123`

## 常见问题

### 登录接口 500 / OperationalError

多数是 MySQL 未启动。确认：

```bat
netstat -ano | findstr ":3306" | findstr LISTENING
```

无输出时按上文重新启动 `mysqld.exe --console`。

### 密码不对

以 `config.yaml` 的 `DB_PASSWORD`（当前 `testhub123`）为准，不要使用旧文档里的其它密码。

### Redis 连不上

确认 `6379` 在监听，且工作目录下存在 `redis.windows.conf`。

### 端口被占用

```bat
netstat -ano | findstr ":3306"
netstat -ano | findstr ":6379"
netstat -ano | findstr ":8001"
netstat -ano | findstr ":3002"

taskkill /F /PID <进程ID>
```

### 前端能开、接口全失败

检查 Vite 代理目标是否指向 `8001`，以及后端是否已启动：

```bat
netstat -ano | findstr ":8001" | findstr LISTENING
```

## 文件位置速查

- MySQL：`D:\mysql-8.0.46-winx64\`
- Redis：`D:\project\testhub\redis-windows\`
- 项目根：`D:\project\testhub\`
- 虚拟环境：`D:\project\testhub\venv\`
- 配置：`D:\project\testhub\config.yaml`
- 后端启动脚本：`D:\project\testhub\start_backend.py`
- 前后端一键脚本：`D:\project\testhub\start.bat`

## 日常建议

1. 首次 / 开机后：先起 MySQL、Redis，再运行 `start.bat`
2. 日常开发：MySQL、Redis 保持运行，只重启前后端即可
3. 单独调试后端：`venv\Scripts\python.exe start_backend.py --port 8001`
