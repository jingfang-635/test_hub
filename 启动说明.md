# TestHub 本地启动说明

## 📋 服务依赖

本项目需要以下服务运行:

1. **MySQL 8.0.46** - 数据库 (端口：3306)
2. **Redis (Windows)** - 缓存服务 (端口：6379)
3. **后端服务** - Django + Uvicorn (端口：8001)
4. **前端服务** - Vue3 + Vite (端口：3002)

## 🚀 快速启动

### 方式一：一键启动所有服务 (推荐)

双击运行:
```
start-all-services.bat
```

该脚本会自动:
- 启动 MySQL 数据库
- 启动 Redis 缓存
- 启动前后端服务
- 验证所有服务状态

### 方式二：手动分步启动

#### 1. 启动 MySQL
```bash
cd D:\mysql-8.0.46-winx64\bin
.\mysqld.exe --console
```

MySQL 连接信息:
- 主机：`127.0.0.1:3306`
- 用户：`root`
- 密码：`Testhub@2026`
- 数据库：`testhub`

#### 2. 启动 Redis
```bash
cd d:\project\testhub\redis-windows
.\redis-server.exe redis.windows.conf
```

验证 Redis:
```bash
.\redis-cli.exe ping
# 应返回 PONG
```

#### 3. 启动前后端
```bash
cd d:\project\testhub\test_hub\testhub_platform-main
.\start.bat
```

## 🛑 停止服务

### 方式一：一键停止所有服务

双击运行:
```
stop-all-services.bat
```

### 方式二：手动停止

```bash
# 停止 MySQL
taskkill /F /IM mysqld.exe

# 停止 Redis
taskkill /F /IM redis-server.exe

# 停止 Python 进程 (前后端)
taskkill /F /IM python.exe
```

## 📝 服务配置

### config.yaml 配置
```yaml
# 服务端口
FRONTEND_PORT: 3002
BACKEND_PORT: 8001

# 数据库配置
DB_NAME: testhub
DB_USER: root
DB_PASSWORD: Testhub@2026
DB_HOST: 127.0.0.1
DB_PORT: 3306

# Redis 配置
REDIS_URL: redis://127.0.0.1:6379/0
```

## 🔍 访问地址

启动成功后可访问:

- **前端界面**: http://localhost:3002
- **后端 API**: http://localhost:8001
- **API 文档**: http://localhost:8001/api/docs/
- **管理后台**: http://localhost:8001/admin/

## ⚠️ 常见问题

### MySQL 启动失败
1. 检查端口 3306 是否被占用
2. 检查 data 目录是否存在
3. 查看 MySQL 错误日志

### Redis 启动失败
1. 检查端口 6379 是否被占用
2. 确保 redis.windows.conf 配置文件存在

### 数据库连接失败
1. 确认 MySQL 服务已启动
2. 检查密码是否正确 (Testhub@2026)
3. 确认数据库 testhub 已创建

### 端口被占用
```bash
# 查看端口占用
netstat -ano | findstr :3306
netstat -ano | findstr :6379
netstat -ano | findstr :8001
netstat -ano | findstr :3002

# 停止占用端口的进程
taskkill /F /PID <进程 ID>
```

## 📂 文件位置

- **MySQL**: `D:\mysql-8.0.46-winx64\`
- **Redis**: `d:\project\testhub\redis-windows\`
- **后端代码**: `d:\project\testhub\test_hub\testhub_platform-main\backend\`
- **前端代码**: `d:\project\testhub\test_hub\testhub_platform-main\frontend\`
- **虚拟环境**: `d:\project\testhub\test_hub\testhub_platform-main\venv\`

## 🎯 开发建议

1. **首次启动**: 使用 `start-all-services.bat` 一键启动
2. **日常开发**: 只需启动前后端服务 (数据库和 Redis 保持运行)
3. **调试模式**: 可以单独启动后端进行调试
4. **清理环境**: 使用 `stop-all-services.bat` 停止所有服务

## 📞 技术支持

如遇问题，请查看:
1. 后端日志 (控制台输出)
2. MySQL 错误日志
3. Redis 启动日志
4. 浏览器控制台错误
