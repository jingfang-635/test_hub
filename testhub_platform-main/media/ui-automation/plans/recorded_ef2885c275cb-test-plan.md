# recorded_ef2885c275cb UI 自动化用例计划

> 状态：**已确认** — 用户确认前禁止生成 pages/data/specs 代码

## 基本信息

- 场景：recorded_ef2885c275cb
- 语言：python
- 录制文件：tests/recorded/recorded_ef2885c275cb.py
- 目标 URL：http://mall.lemonban.com:3344/
- 生成时间：2026-08-14 00:06:09

## 页面与 Page Object 规划

| 页面 | 类名 | 主要方法 | 输出文件 |
|------|------|----------|----------|
| 首页 | HomePage | `goto()`、`click_login()`、`click_goods_item()`、`click_add_cart()`、`click_favorite()`、`goto_cart()` | tests/pages/home_page.py |
| 登录页 | LoginPage | `fill_username()`、`fill_password()`、`submit_login()`、`login()` | tests/pages/login_page.py |
| 购物车页 | CartPage | `goto()`、`increase_quantity()`、`decrease_quantity()`、`select_item()`、`click_checkout()` | tests/pages/cart_page.py |
| 结算页 | CheckoutPage | `click_submit_order()`、`submit_order()` | tests/pages/checkout_page.py |

## 用例文件规划

| 功能模块 | Test 类 | 用例 ID 范围 | 输出文件 |
|----------|---------|--------------|----------|
| 用户登录 | TestLogin | TC-001 ~ TC-003 | tests/specs/test_login.py |
| 购物车与收藏 | TestCart | TC-004 ~ TC-006 | tests/specs/test_cart.py |
| 下单结算 | TestOrder | TC-007 ~ TC-009 | tests/specs/test_order.py |

规则：一功能模块一 specs 文件；与 page 模块对齐；禁止多个 Test* 写入同一文件。

## 数据与造数规划

| 字段 | 来源 | 说明 |
|------|------|------|
| 登录手机号/用户名 | `.env` | 录制值 `15183871603`，建议放入 `.env`，禁止硬编码 |
| 登录密码 | `.env` | 录制值 `123456`，与账号配对，放入 `.env` |
| 商品购买数量 | `faker` | 业务上允许 1~99，用例中取固定小值（如 3）用于断言 |
| 购物车数量 | `faker` | 用于购物车列表数量断言，如 `购物车(3)` 改为读取实际 UI 计数 |
| 订单提交状态 | `fixtures` | 断言提交后跳转页面/出现成功提示，不依赖具体文案 |

> 注：录制流程不涉及图形验证码，因此无需 captcha 策略。

## 用例清单

| ID | 类型 | 用例名称 | 前置条件 | 步骤 | 预期结果 | 优先级 | 是否生成 |
|----|------|----------|----------|------|----------|--------|----------|
| TC-001 | 正向 | 登录成功 | 测试环境可访问，账号已注册 | 1. 打开首页 2. 点击登录 3. 输入正确手机号/用户名 4. 输入正确密码 5. 点击登录 | 登录成功，页面跳转至首页，且不再显示“登录”入口（或显示用户信息） | P0 | 是 |
| TC-002 | 负向 | 登录失败 - 错误密码 | 测试环境可访问，账号存在 | 1. 打开登录页 2. 输入正确手机号 3. 输入错误密码 4. 点击登录 | 登录失败，页面提示“密码错误”或停留在登录页 | P1 | 是 |
| TC-003 | 负向 | 登录失败 - 空账号 | 测试环境可访问，账号存在 | 1. 打开登录页 2. 密码输入任意值 3. 点击登录 | 登录失败，提示账号不能为空，停留在登录页 | P2 | 是 |
| TC-004 | 正向 | 添加商品到购物车 | 已登录，首页存在商品列表 | 1. 打开首页 2. 点击第一个商品 3. 点击“加入购物车” 4. 重复点击 3 次 | 购物车图标数量变为 3，且购物车内有对应商品 | P0 | 是 |
| TC-005 | 正向 | 购物车数量调整 | 已登录，购物车已有至少 1 件商品 | 1. 打开购物车 2. 点击“+”增加数量 3. 点击“-”减少数量 | 购物车数量随点击变化，且数量不为负数 | P1 | 是 |
| TC-006 | 正向 | 收藏商品 | 已登录，商品详情页可操作 | 1. 打开商品详情页 2. 点击“收藏商品” | 收藏成功，出现“取消收藏”或类似状态变化 | P2 | 是 |
| TC-007 | 正向 | 提交订单成功 | 已登录，购物车有选中商品 | 1. 进入购物车 2. 勾选商品 3. 点击“结算” 4. 点击“提交订单” | 跳转至订单确认/支付页面，订单生成成功 | P0 | 是 |
| TC-008 | 权限 | 未登录访问购物车 | 未登录状态 | 1. 直接访问购物车链接或点击购物车入口 | 被重定向至登录页，或提示需要登录 | P1 | 是 |
| TC-009 | 边界 | 购物车数量最小值边界 | 已登录，购物车有商品 | 1. 打开购物车 2. 连续点击“-”直至 0 或置灰 | 数量最低为 1 或“-”按钮置灰，数量不出现负数/报错 | P1 | 是 |

类型：`正向` / `负向` / `边界` / `权限`

## 选择器优化建议

| 原选择器（录制） | 建议优化 |
|------------------|----------|
| `page.get_by_text("+")` | 改为 `page.get_by_role("button", name="+")` 或基于商品项的 `locator`，避免匹配页面多个 `+` 元素 |
| `page.get_by_text("购物车(0)")` | 购物车数量会变化，建议使用固定 `链接` locator（如 `get_by_role("link").filter(has_text=...)`）或基于 `aria-label` |
| `page.get_by_role("link", name="登录").nth(1)` | 使用页面容器/父级定位，或给登录按钮加 test-id |
| `.goods-img > img` | 建议结合商品项容器 `locator(".goods-list > .list-con > div")` 限定范围，避免页面其他图片干扰 |
| `page.get_by_text("-", exact=True)` | 使用更具体的 `button` role 或 `aria-label` |
| `get_by_role("checkbox").nth(1)` | 改用购物车商品条目的 `checkbox` 定位，如 `cart_item.locator("checkbox")` |

## 待确认项

- [ ] 用例覆盖是否完整（登录、购物车、下单、权限、边界）
- [ ] 造数方式是否可行（账号密码放入 `.env`，商品数量使用 faker）
- [ ] 是否需要 API 前置造数（如预置购物车商品，或用户必须手动准备）
- [ ] Page Object 拆分是否合理（首页/登录页/购物车页/结算页）
- [ ] 用例是否按模块拆分到独立 specs 文件（login/cart/order）

## 确认记录

- 确认人：
- 确认时间：
- 备注：