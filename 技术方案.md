# TestHub 平台重构升级技术方案

> 基于《TestHub 星球专属版正式发布！平台重构升级，这些变化值得关注！》文章整理

## 一、项目背景与目标

### 1.1 背景
当前 TestHub 平台（开源版）存在以下痛点：
- **部署复杂**：依赖 Celery + Redis Broker + Worker，依赖包多达 192 行，安装易报错冲突
- **配置分散**：前后端配置分离（`backend/.env`、`frontend/.env`），易不同步
- **监控缺失**：无接口性能数据，优化方向缺乏数据支撑
- **通知能力弱**：通知格式写死在代码中，不支持邮件、不支持变量替换
- **定时任务分散**：各模块（api_testing、ui_automation）各自管理，逻辑重复、通知不统一

### 1.2 目标
- 让部署更省心：精简依赖、一键启动、统一配置
- 让功能更实用：新增性能监控、通知模板系统
- 让操作更顺手：UI/AI 模块细节优化、定时任务统一调度

---

## 二、架构升级：Celery → Django-Q2 ⭐

### 2.1 替换原因

| 痛点 | Celery 现状 |
|------|------------|
| 部署复杂 | 需额外 Redis Broker + Celery Worker |
| 依赖繁重 | 需安装 celery、redis、billiard 等 |
| 配置繁琐 | 需配置 broker_url、result_backend 等 |
| 资源占用高 | 独立 Worker 进程占用较多系统资源 |

> 注：Windows 环境下 Celery 必须使用 `--pool=solo`，否则 prefork 模式触发 `[WinError 5]` 权限错误并崩溃。

### 2.2 Django-Q2 优势

| 对比项 | Celery | Django-Q2 |
|--------|--------|-----------|
| 配置复杂度 | 高（需配置 Broker） | 低（Django 内置配置） |
| 部署难度 | 需独立启动 Worker | 一条命令 `python manage.py qcluster` |
| 依赖数量 | 多（redis/celery/billiard） | 少（django-q2 + redis） |
| 任务监控 | 需额外安装 Flower | Admin 后台自带监控 |
| 内存占用 | 较高 | 较低 |

### 2.3 实施步骤

1. **依赖替换**
   - 移除：`celery==5.3.4`、`billiard`、`kombu`
   - 新增：`django-q2>=1.6.0`
   - 保留：`redis`（仍作为 Django-Q2 的 broker）

2. **配置改造**（`settings.py`）
   ```python
   INSTALLED_APPS = [
       # ...
       'django_q',
   ]

   Q_CLUSTER = {
       'name': 'testhub',
       'workers': 4,
       'recycle': 500,
       'timeout': 60,
       'retry': 120,
       'queue_limit': 50,
       'bulk': 10,
       'orm': 'default',  # 使用数据库存储任务结果，无需额外 broker
   }
   ```

3. **启动方式对比**
   ```bash
   # 原 Celery 方式（Windows 必须 solo 模式）
   celery -A backend worker -l info --pool=solo

   # 新 Django-Q2 方式
   python manage.py qcluster
   ```

4. **任务迁移**
   - 将原 `@shared_task` / `@app.task` 装饰器改为 Django-Q2 的 `async_task` 调用
   - 原 `task.delay()` → `async_task('module.task_func', args)`
   - 定时任务改用 `Schedule.objects.create()` 创建

### 2.4 风险与兼容
- Celery 的链式任务（chain）、组任务（group）需用 Django-Q2 的 `async_chain`、`async_iter` 替代
- 任务结果查询方式由 `AsyncResult` 改为 `Task` 模型查询
- Windows 环境无需再强制 solo 模式，Django-Q2 原生兼容

---

## 三、新增功能：性能监控系统 ⭐

### 3.1 问题背景
- 用户反馈"平台响应慢"，开发者无法定位慢接口
- 缺乏数据支撑优化方向

### 3.2 数据模型设计

**1. RequestPerformanceLog（请求性能日志）**

```python
class RequestPerformanceLog(models.Model):
    path = models.CharField(max_length=500, verbose_name='请求路径')
    method = models.CharField(max_length=10, verbose_name='请求方法')
    response_time = models.FloatField(verbose_name='响应时间(ms)')
    status_code = models.IntegerField(verbose_name='状态码')
    user = models.ForeignKey(User, on_delete=models.SET_NULL, null=True)
    ip_address = models.GenericIPAddressField(null=True, blank=True)
    user_agent = models.CharField(max_length=500, blank=True)
    created_at = models.DateTimeField(auto_now_add=True)
```

记录内容：请求路径/方法、响应时间(ms)、状态码、用户信息、IP、User-Agent。

**2. PerformanceStatistics（性能统计）**

```python
class PerformanceStatistics(models.Model):
    date = models.DateField(unique=True, verbose_name='日期')
    total_requests = models.IntegerField(default=0, verbose_name='总请求数')
    avg_response_time = models.FloatField(default=0, verbose_name='平均响应时间(ms)')
    max_response_time = models.FloatField(default=0, verbose_name='最大响应时间(ms)')
    min_response_time = models.FloatField(default=0, verbose_name='最小响应时间(ms)')
    error_count = models.IntegerField(default=0, verbose_name='错误请求数')
    slow_requests = models.IntegerField(default=0, verbose_name='慢请求数(>1s)')
```

统计指标：每日总请求数、平均/最大/最小响应时间、错误请求数(4xx/5xx)、慢请求数(>1s)。

### 3.3 中间件实现

新增性能记录中间件，拦截所有请求并记录：

```python
class PerformanceLoggingMiddleware:
    def __init__(self, get_response):
        self.get_response = get_response

    def __call__(self, request):
        start_time = time.time()
        response = self.get_response(request)
        response_time = (time.time() - start_time) * 1000  # ms

        RequestPerformanceLog.objects.create(
            path=request.path,
            method=request.method,
            response_time=response_time,
            status_code=response.status_code,
            user=getattr(request, 'user', None) if request.user.is_authenticated else None,
            ip_address=get_client_ip(request),
            user_agent=request.META.get('HTTP_USER_AGENT', '')[:500],
        )
        return response
```

### 3.4 Admin 可视化（admin_performance.py）

- **颜色标记**：慢请求(>1s)红色、中等(>500ms)橙色、正常绿色
- **状态码标记**：5xx 红色、4xx 橙色、2xx 绿色
- **统计面板**：错误率、慢请求率一目了然

### 3.5 自动聚合定时任务

```python
# 每日凌晨聚合前一天的性能数据
Schedule.objects.create(
    name='每日性能统计聚合',
    func='apps.core.tasks.aggregate_daily_performance_stats',
    schedule_type='D',  # Daily
    next_run=now() + timedelta(days=1),
    repeats=-1  # 永久重复
)

# 每30分钟聚合当天的实时数据
Schedule.objects.create(
    name='实时性能统计聚合',
    func='apps.core.tasks.aggregate_realtime_performance_stats',
    schedule_type='I',  # Interval
    minutes=30,
    repeats=-1
)
```

---

## 四、新增功能：通知模板系统 ⭐

### 4.1 问题背景
原通知功能限制：
- 通知格式写死在代码里，不支持自定义
- 不支持邮件通知
- 不支持变量替换

### 4.2 NotificationTemplate 模型

```python
class NotificationTemplate(models.Model):
    TEMPLATE_TYPE_CHOICES = [
        ('markdown', 'Markdown'),
        ('html', 'HTML'),
        ('text', '纯文本'),
    ]

    name = models.CharField(max_length=100, verbose_name='模板名称')
    template_type = models.CharField(max_length=20, choices=TEMPLATE_TYPE_CHOICES)
    subject = models.CharField(max_length=200, blank=True, verbose_name='邮件主题')
    content = models.TextField(verbose_name='模板内容')
    variables = models.JSONField(default=list, blank=True, verbose_name='模板变量')
    is_default = models.BooleanField(default=False, verbose_name='是否默认模板')
    is_active = models.BooleanField(default=True, verbose_name='是否启用')
```

### 4.3 支持的模板变量

```python
# 定时任务相关变量
{{task_name}}          # 任务名称
{{status_text}}        # 执行状态（成功/失败）
{{execution_time}}     # 执行时间
{{task_type}}          # 任务类型

# 测试执行相关变量
{{title}}              # 测试标题
{{tester}}             # 测试人员
{{total_cases}}        # 用例总数
{{passed_cases}}       # 通过用例数
{{failed_cases}}       # 失败用例数
{{error_cases}}        # 错误用例数
{{skipped_cases}}      # 跳过用例数
{{runtime}}            # 执行时长
{{begin_time}}         # 开始时间
```

### 4.4 通知配置增强（UnifiedNotificationConfig）

```python
class UnifiedNotificationConfig(models.Model):
    # 原有配置
    webhook_url = models.URLField(max_length=500, blank=True)
    secret = models.CharField(max_length=200, blank=True)
    # 新增邮件配置
    email_recipients = models.JSONField(default=list, blank=True)
    email_attach_report = models.BooleanField(default=False)
    # 关联通知模板
    notification_template = models.ForeignKey(
        'NotificationTemplate',
        on_delete=models.SET_NULL,
        null=True,
        blank=True
    )
```

**邮件收件人支持两种格式**：
- 用户 ID：自动获取用户邮箱
- 邮箱地址：直接填写

> 配置入口：后台管理 → 核心模块 → 通知模板

---

## 五、新增 scheduler 统一调度模块 ⭐

### 5.1 独立原因
原项目定时任务分散在 api_testing、ui_automation 等模块中：
- 代码重复，每个模块都有自己的任务管理逻辑
- 通知配置不统一
- 无法统一管理所有定时任务

### 5.2 模块结构

```
scheduler/
├── admin.py              # Admin 后台配置
├── apps.py               # 应用配置
├── migrations/           # 数据库迁移
├── models.py             # ScheduleConfig 扩展模型
├── serializers.py        # API 序列化器
├── task_executor.py      # 统一任务执行器
├── urls.py               # API 路由
└── views.py              # API 视图
```

### 5.3 核心模型 ScheduleConfig

扩展 Django-Q 的 Schedule 模型，增加业务配置：

```python
class ScheduleConfig(models.Model):
    MODULE_CHOICES = [
        ('API', 'API测试'),
        ('UI', 'UI自动化'),
        ('APP', 'APP自动化'),
    ]
    TASK_TYPE_CHOICES = [
        ('API_TEST_SUITE', 'API测试套件'),
        ('API_REQUEST', 'API请求'),
        ('UI_TEST_SUITE', 'UI测试套件'),
        ('UI_TEST_CASE', 'UI测试用例'),
        ('APP_TEST_SUITE', 'APP测试套件'),
        ('APP_TEST_CASE', 'APP测试用例'),
    ]
    schedule = models.OneToOneField(Schedule, ...)
    module = models.CharField(max_length=20, choices=MODULE_CHOICES)
    task_type = models.CharField(max_length=30, choices=TASK_TYPE_CHOICES)
    project_id = models.IntegerField()
    target_id = models.IntegerField()
    environment_id = models.IntegerField()
    task_config = models.JSONField(default=dict)
    # 通知配置
    notify_on_success = models.BooleanField(default=False)
    notify_on_failure = models.BooleanField(default=True)
    notify_on_email = models.BooleanField(default=False)
    notify_on_webhook = models.BooleanField(default=False)
    notification_configs = models.ManyToManyField('core.UnifiedNotificationConfig')
    notification_template = models.ForeignKey('core.NotificationTemplate', ...)
    # 统计信息
    last_run_time = models.DateTimeField(null=True, blank=True)
    success_count = models.IntegerField(default=0)
    failure_count = models.IntegerField(default=0)
```

### 5.4 统一任务创建入口

```python
def create_scheduled_task(
    name,
    module,
    task_type,
    schedule_type,
    target_id=None,
    project_id=None,
    environment_id=None,
    task_config=None,
    cron=None,
    minutes=None,
    notification_template=None,
    ...
):
    """创建定时任务的统一入口"""
    func = get_task_function(task_type)
    schedule = Schedule.objects.create(
        name=name,
        func=func,
        schedule_type=schedule_type,
        cron=cron,
        minutes=minutes,
        ...
    )
    config = ScheduleConfig.objects.create(...)
    return schedule, config
```

### 5.5 任务控制方法

```python
def pause(self):
    """暂停任务"""
    self.status = 'PAUSED'
    self.schedule.next_run = None
    self.save()

def resume(self):
    """恢复任务"""
    self.status = 'ACTIVE'
    self.schedule.next_run = timezone.now()
    self.save()

def execute_now(self):
    """立即执行任务"""
    from apps.scheduler.task_executor import execute_task
    return execute_task(self.schedule_id)
```

---

## 六、统一配置文件 config.yaml ⭐

### 6.1 问题背景
原项目配置分散：
- 后端配置：`backend/.env`
- 前端配置：`frontend/.env`
- 配置不同步，容易出错

### 6.2 统一配置方案

根目录新增 `config.yaml`：

```yaml
# 服务端口配置
server:
  frontend_port: 3000
  backend_port: 8000
  backend_host: "127.0.0.1"
  frontend_url: "http://localhost:3000"
  debug: false
  secret_key: xxxxxxx

# 数据库配置
database:
  name: testhub
  user: root
  password: ""
  host: "127.0.0.1"
  port: 3306

# Redis 配置
redis:
  url: "redis://127.0.0.1:6379/"
  redis_db: 0
  cache_db: 1
  session_db: 2

# JWT 配置
jwt:
  access_token_lifetime: 30
  refresh_token_lifetime: 7
```

### 6.3 配置加载
- 后端 Django 启动时读取 `config.yaml` 并注入 settings
- 前端构建时从 `config.yaml` 读取端口配置生成代理配置
- 修改端口只需改一处，前后端同步生效

---

## 七、项目结构调整

### 7.1 结构对比

**原结构**：
```
testhub_platform/
├── apps/                    # Django 应用（根目录）
├── backend/                 # Django 配置
├── frontend/                # Vue 前端
├── manage.py
└── requirements.txt
```

**新结构**：
```
testhub_platform/
├── backend/
│   ├── apps/                # Django 应用（移到 backend 下）
│   ├── backend/             # Django 配置
│   ├── manage.py            # 管理脚本
│   ├── requirements.txt     # Python 依赖
│   ├── start.sh             # 启动脚本（Linux/Mac）
│   └── start.bat            # Windows 启动脚本
├── frontend/                # Vue 前端
├── config.yaml              # 统一配置文件 ⭐
├── expand/                  # 扩展工具
│   └── allure/              # Allure 测试报告
└── docs/                    # 文档目录
```

### 7.2 结构变化意义
- **后端独立**：backend 目录可独立部署
- **配置统一**：根目录 config.yaml 统一管理
- **扩展分离**：扩展工具（如 Allure）单独管理
- **文档集中**：docs 目录统一存放文档

---

## 八、技术栈升级

| 技术组件 | 原版本 | 新版本 | 升级说明 |
|---------|--------|--------|---------|
| Django | 4.2.7 | 6.0.2 | 性能提升、新特性 |
| Celery | 5.3.4 | ❌ 移除 | 替换为 Django-Q2 |
| django-q2 | ❌ | 1.6.0+ | 轻量级任务队列 |
| mysqlclient | 2.2.7 | 2.2.7 | 保持不变 |
| PyMySQL | 1.1.0 | >=1.1.2 | 版本更新 |

> ⚠️ Django 6.0 要求 Python 3.10+，需确认运行环境 Python 版本。

---

## 九、部署简化

### 9.1 依赖精简
- `requirements.txt` 从 192 行精简到 49 行
- 减少约 75% 的依赖包，降低安装报错/冲突概率

### 9.2 无需创建迁移文件
- 遵循 Django 最佳规范，所有迁移文件随代码上传
- 表结构变动只需执行 `python manage.py migrate`
- 无需手动 `makemigrations`

### 9.3 启动方式简化

**原部署步骤**（4 个服务窗口）：
1. 启动前端
2. 启动 Django Server
3. 启动 Celery Worker
4. 启动定时任务 scheduler

**新部署步骤**（2 步）：
1. 启动前端
2. 启动聚合后台：`./start.sh`（Windows 为 `.\start.bat`）
   - 聚合：Django Server + Django-Q + Scheduler
   - 无需激活虚拟环境

### 9.4 端口配置灵活
- 修改前后台端口号只需改 `config.yaml` 一处
- 支持修改 MySQL 端口号

---

## 十、各模块功能优化

### 10.1 UI 自动化模块

| 优化项 | 说明 |
|--------|------|
| 元素筛选 | 支持根据页面选择及元素名称筛选元素 |
| URL 跳转操作 | 测试用例操作步骤新增"跳转URL"操作类型 |
| 元素管理增强 | 增加一键复制、删除和拖拽元素功能 |
| URL 断言 | 断言类型增加"URL包含"断言 |
| 必填校验 | 编辑元素页面增加必填项展示和脱焦校验 |

### 10.2 定时任务配置细化
得益于 Django-Q，触发器完美支持：每小时、每天、每周、每月、每年等。

### 10.3 AI 智能模块优化

| 优化项 | 说明 |
|--------|------|
| 执行前预检机制 | 失败立刻终止并明确报错，避免先开浏览器再连续空转多轮失败 |
| 任务拆分兜底 | 增加本地兜底机制，避免连接模型失败时只拆分为一条 |
| 结果标记优化 | 增加最大失败重试 3 次限制；测试完成不再调用统一标记方法，避免 AI 执行幻觉 |

### 10.4 Bug 修复
- 修复部分场景下 AI 生成用例后下载内容不全或生成记录详情内容为空问题
- 修复 UI 自动化测试模块多处按钮名称、提示信息等国际化翻译问题

---

## 十一、实施计划与风险控制

### 11.1 实施阶段

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| 阶段一 | 架构升级（Celery → Django-Q2）+ 依赖精简 | 高 |
| 阶段二 | 项目结构调整 + 统一配置 config.yaml | 高 |
| 阶段三 | 性能监控系统（模型 + 中间件 + Admin + 聚合任务） | 中 |
| 阶段四 | 通知模板系统 + scheduler 统一调度模块 | 中 |
| 阶段五 | UI/AI 模块功能优化 + Bug 修复 | 中 |
| 阶段六 | 启动脚本（start.sh/start.bat）+ 部署文档 | 低 |

### 11.2 风险与应对

| 风险 | 应对措施 |
|------|---------|
| Celery 任务迁移遗漏 | 全局搜索 `@shared_task`、`@app.task`、`.delay()` 逐一替换 |
| Django 6.0 兼容性 | 确认 Python 版本≥3.10，第三方包支持 Django 6.0 |
| 数据迁移文件策略变更 | 确保迁移文件完整上传，CI 中校验 |
| Windows 环境差异 | start.bat 兼容 Windows 路径与编码 |
| 性能日志数据量增长 | 中间件排除静态资源/健康检查接口，定期清理历史日志 |

### 11.3 验收标准
- [ ] `requirements.txt` 精简至 49 行左右，无安装冲突
- [ ] `./start.sh` / `.\start.bat` 一键启动 Django + QCluster + Scheduler
- [ ] `config.yaml` 修改端口后前后端同步生效
- [ ] 性能监控 Admin 可见慢请求/错误率统计
- [ ] 通知模板支持变量替换及邮件发送
- [ ] scheduler 统一管理 API/UI/APP 三类定时任务
- [ ] UI 自动化新增 URL 跳转/断言、元素筛选/拖拽功能可用
- [ ] AI 模块预检机制生效，失败立即终止
